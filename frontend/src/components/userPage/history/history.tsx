import { History } from '../../../constants';

type Props = {
    history: History
}

const HistoryWidget = ({ history } : Props) => {

    return (
        <div>history</div>
    );
}

export default HistoryWidget;